[buildout]
extends =
    ../profiles/base.cfg
    ../versions/ztk-versions-1.1.4.cfg

[test]
defaults = [
    '-s', '${buildout:package}.wsgi',
    '-s', '${buildout:package}.custom',
    '-s', '${buildout:package}.static',
    ]
