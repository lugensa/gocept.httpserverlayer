[buildout]
extends =
    ../profiles/base.cfg
    ../versions/plone-4.0.10.cfg
extras = zope2testcase
test-package = zope2testcase.tests.zope212

[test]
# XXX why does the version of zope.testrunner pinned here need the explicit
# --test-pattern?
defaults = ['-s', '${buildout:package}.${buildout:test-package}',
            '--tests-pattern', 'test_']
